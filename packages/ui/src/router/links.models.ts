export const enum Links {
  Home = '/', // Flows visualization
  SourceCode = '/code', // Flows source code
  DataMapper = '/datamapper',
  AgentConfiguration = '/agent-configuration', // Citrus agent service configuration
  TestResults = '/test-results', // Citrus test results provided by the agent service
  About = '/about',
  Beans = '/beans',
  Rest = '/rest',
  Metadata = '/metadata',
  PipeErrorHandler = '/pipe-error-handler',
  Catalog = '/catalog',
  Settings = '/settings',
}

export const enum ExternalLinks {
  GitHub = 'https://github.com/KaotoIO/kaoto',
}
