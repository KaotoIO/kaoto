- route:
    from:
      id: from-4257
      uri: timer:template
      parameters:
        timerName: timer-name
        repeatCount: "5"
      steps:
        - setBody:
        # wrong id placement - creates unknown node
        - id: setBody-3518
        # wrong id placement
        - log:
            id: log-4225
            message: ${body}
    id: route-8484
